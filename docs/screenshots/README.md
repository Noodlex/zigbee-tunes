# Screenshots

Drop the images referenced by the root `README.md` here:

- `devices.png` — the Devices grid (hero shot). A wide capture with a few
  cards and, ideally, a selection + the edit panel open.
- `edit.png` — bulk-editing color temperature, showing the native-range /
  "safe intersection" hints.

Recommended: PNG, ~1200–1600px wide, light **or** dark theme (be
consistent). Crop out the browser chrome. Redact any personal data
(real IEEE addresses are fine; broker IPs are not shown in the UI).

Optional extras worth adding and linking from the README:
`dashboard.png`, `customizations.png`.
